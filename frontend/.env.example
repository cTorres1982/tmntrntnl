# Base URL of the JobTracker.Api backend (server-side only — never exposed to the browser).
BACKEND_API_URL=http://localhost:5080

# Dev-mode tenant header the backend's TenantContextMiddleware reads (see backend README).
DEV_ORGANIZATION_ID=11111111-1111-1111-1111-111111111111
